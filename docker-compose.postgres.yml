services:
  lenra_postgres:
    container_name: lenra_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: lenra_dev
      POSTGRES_PASSWORD: postgres:14.7-bullseye
    networks:
      - lenra_dev
    restart: always
